name: CI
on: 
    push:
        branches: main
jobs:
    build: 
        runs-on: ubunutu-latest

        steps: 
            - uses: actions/checkout@v4
            - name: run a one line script
              run: echo "hello world , "
  